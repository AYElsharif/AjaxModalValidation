@model Contact
@{  ViewData["Title"] = "Contact form"; }

<!-- Modal placeholder -->
<div id="modal-placeholder"></div>

<!-- Button trigger modal -->
<button type="button" class="btn btn-primary" data-bs-toggle="ajax-modal" data-url="@Url.Action("Contact")">Add contact</button>